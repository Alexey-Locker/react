import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
    wrapper: {
        minWidth: "100vw",
        marginBottom: theme.spacing(1),
    },
    list: {
        padding: "0",
        backgroundColor: "#b1b3ff33",
    }
}))